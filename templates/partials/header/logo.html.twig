{% set _logo = Image(theme.theme_mod('custom_logo')) %}
<figure class="custom-logo-wrapper">
    <a href="{{ site.link }}" title="{{ site.title }}">
        <img src="{{ _logo.src('large') }}" alt="{{ _logo.alt?? site.title }}" title="{{ _logo.title }}" class="custom-logo" />
    </a>
</figure>
